import '../components/builder.settings';
import '../styles/slider.scss';
import '../styles/card.scss';

function MyApp({ Component, pageProps }) {
	return (
		<>
			<Component {...pageProps} />
		</>
	);
}

export default MyApp;
